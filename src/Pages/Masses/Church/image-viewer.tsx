/* import { ImageWithPlaceholder } from "@/components/ImageWithPlaceholder";
import { CSSProperties } from "react";

export const ImageViewer = ({ links, style }: { links: string[]; style: CSSProperties }) => {
  return (
    <div>
      <ImageWithPlaceholder
        className="bg-cover w-full object-cover h-[50%] rounded-2xl shadow-sm mt-4"
        style={style}
        placeholderSrc={placeholderImage}
        src={church?.kep ?? ""}
        alt={church?.nev ?? ""}
      />
    </div>
  );
};
 */